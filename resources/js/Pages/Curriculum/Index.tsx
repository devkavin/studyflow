import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, useForm } from '@inertiajs/react';
export default function Index({subjects}:{subjects:any[]}) { const s=useForm({name:'',color:'#3b82f6',priority:3}); const imp=useForm({input:''}); return <AuthenticatedLayout><Head title="Curriculum" /><h1 className='mb-4 text-2xl font-semibold'>Curriculum</h1><form onSubmit={e=>{e.preventDefault();s.post(route('subjects.store'));}} className='mb-4 flex gap-2'><input aria-label='Subject name' className='rounded border p-2' value={s.data.name} onChange={e=>s.setData('name',e.target.value)} /><button className='rounded bg-indigo-600 px-3 py-2 text-white'>Add Subject</button></form><form onSubmit={e=>{e.preventDefault();imp.post(route('curriculum.import'));}} className='mb-6 flex gap-2'><input aria-label='Quick import' className='flex-1 rounded border p-2' value={imp.data.input} onChange={e=>imp.setData('input',e.target.value)} /><button className='rounded bg-slate-800 px-3 py-2 text-white'>Quick Add</button></form><div className='space-y-3'>{subjects.map((subject)=><div key={subject.id} className='rounded bg-white p-4 shadow'><h2 className='font-medium'>{subject.name}</h2><p className='text-sm text-slate-500'>{subject.topics?.length ?? 0} topics</p></div>)}</div></AuthenticatedLayout>; }
